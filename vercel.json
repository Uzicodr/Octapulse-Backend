{
  "buildCommand": "pip install --upgrade pip && pip install -r requirements.txt",
  "functions": {
    "api/app.py": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/app.py"
    }
  ]
}
